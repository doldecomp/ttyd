#ifndef __GLOBAL_H_
#define __GLOBAL_H_

#include "dolphin.h" // include SDK symbols.

#define OS_PANIC_HARDCODED(file, line, msg) OSPanic(file, line, msg)

#include "variables.h"
#include "functions.h"

#endif
